"$schema" = "./tsumiki.schema.json"

# ── General Settings ──────────────────────────────────
[general]
debug = false                     # Enable verbose logging
auto_restart = true               # Automatically restart on crash
restart_delay = 1500              # Delay before restart (ms)
multi_monitor = false             # Per-monitor bar instances
tooltips = true                   # Enable widget tooltips
check_updates = false              # Check for Tsumiki updates
monitor_styles = true             # Watch and reload SCSS changes

# ── Layout ────────────────────────────────────────────
# Widget placement in the bar. Reference widgets by name,
# or use @group:N / @collapsible:N for groups (zero-based index).

[layout]
left_section = ["workspaces", "window_title"]
middle_section = ["date_time"]
right_section = [
    "@group:sys-stats",
    "system_tray",
    "volume",
    "battery",
    "power",
]

# ── Widget Groups ────────────────────────────────────
# Group related widgets together with shared styling.

[[widget_groups]]
id = "sys-stats"
widgets = ["updates", "network_usage"]
spacing = 4
style_classes = ["bordered"]

# ── Collapsible Groups ───────────────────────────────
# Hide utility widgets behind a toggle icon.

[[collapsible_groups]]
widgets = ["ocr", "screenshot", "recorder"]
spacing = 4
icon = "󰒓"
tooltip = "Utility Tools"
style_classes = ["utility-tools"]

# ── Modules ──────────────────────────────────────────

[modules.bar]
layer = "top"                     # "top" | "overlay" | "bottom" | "background"
location = "top"                  # "top" | "bottom"
auto_hide = false
auto_hide_timeout = 3000

[modules.notification]
enabled = true
anchor = "top-right"
auto_dismiss = true
dnd_on_screencast = true
transition_type = "slide-left"
transition_duration = 350

[modules.notification.timeout]
low = 3000
normal = 8000
critical = 15000

[modules.notification.persist]
enabled = true
max_count = 200

[modules.dock]
enabled = false                   # Enable to show the dock
behavior = "intellihide"
icon_size = 40
layer = "top"

[modules.overview]
enabled = false
layer = "top"
anchor = "center"
transition_type = "crossfade"
transition_duration = 350

[modules.osd]
enabled = false
timeout = 3000
anchor = "bottom-center"
orientation = "horizontal"
osds = ["brightness", "volume"]

[modules.launcher]
enabled = false
tooltip = true
icon_size = 35
ignored = []
anchor = "center"
width = 280
height = 320
layout = "grid"
grid_columns = 3
plugins_enabled = true
plugins_dir = ""

[modules.desktop_clock]
enabled = false
layer = "bottom"
anchor = "bottom-right"

[modules.desktop_quotes]
enabled = false
anchor = "bottom-right"
interval = 600

[modules.screen_corners]
enabled = false
size = 20

# ── Widgets ──────────────────────────────────────────

# --- Workspaces ---
[widgets.workspaces]
count = 10
hide_unoccupied = true
reverse_scroll = false
style = "numbered"                # "numbered" | "pill" | "icon" | "minimal" | "underline" | "bubble"
empty_scroll = false
label_format = "{id}"
icon_map = {}
ignored = [-99]

# --- Window Title ---
[widgets.window_title]
icon = true
truncation = true
truncation_size = 50
tooltip = true
fallback = "class"                # "class" | "title"

# --- Date & Time ---
[widgets.date_time]
date_format = "%b %d %H:%M"
calendar = true
clock_format = "12h"              # "12h" | "24h"

[widgets.date_time.notification]
enabled = true
count = true
hide_count_on_zero = true

# --- Volume ---
[widgets.volume]
tooltip = true
step_size = 5

# --- Battery ---
[widgets.battery]
hide_percent_when_full = true
hide_when_missing = true
icons = ["", "", "", "", ""]
tooltip = true
label_format = "{icon} {percent}"

[widgets.battery.notifications]
low_threshold = 10
full_battery = false
low_battery = false
charging = false

# --- System Tray ---
[widgets.system_tray]
icon_size = 16
hide_when_empty = false
ignored = []
hidden = []

# --- Power Menu ---
[widgets.power]
icon = "󰐥"
tooltip = true
items_per_row = 3
icon_size = 100
confirm = true                    # Show confirmation before executing

[widgets.power.item_shortcuts]
shutdown = "s"
reboot = "r"
hibernate = "h"
suspend = "u"
lock = "l"
logout = "o"

[widgets.power.buttons]
shutdown = "systemctl poweroff"
reboot = "systemctl reboot"
hibernate = "systemctl hibernate"
suspend = "systemctl suspend"
lock = "loginctl lock-session"
logout = "loginctl terminate-user $USER"

# --- Quick Settings ---
[widgets.quick_settings]
hover_reveal = false

[widgets.quick_settings.user]
avatar = "~/.face"
name = "system"
distro_icon = true

[widgets.quick_settings.controls]
sliders = ["brightness", "volume"]

[widgets.quick_settings.media]
enabled = true
truncation_size = 30

[widgets.quick_settings.shortcuts]
enabled = true

[[widgets.quick_settings.shortcuts.items]]
icon = ""
label = "Terminal"
command = "kitty"
tooltip = "Open terminal"
icon_size = 18

[[widgets.quick_settings.shortcuts.items]]
icon = ""
label = "Browser"
command = "firefox"
tooltip = "Launch Firefox"
icon_size = 18

# --- CPU ---
[widgets.cpu]
show_icon = true
icon = ""
tooltip = true
mode = "circular"                 # "label" | "graph" | "circular"
graph_length = 4
temperature_unit = "celsius"

# --- Memory ---
[widgets.memory]
show_icon = true
icon = ""
tooltip = true
mode = "circular"
unit = "gb"

# --- Updates ---
[widgets.updates]
show_icon = true
available_icon = "󰏗"
no_updates_icon = "󰏖"
os = "arch"
interval = 3600
tooltip = true
terminal = "kitty"
flatpak = true
label = true

# --- Weather ---
[widgets.weather]
location = "kathmandu"
label_format = "{temperature} {condition}"
tooltip = true
temperature_unit = "celsius"
wind_speed_unit = "kmh"
interval = 86400
provider = "open-meteo"           # "open-meteo" | "wttr"

# --- Network Usage ---
[widgets.network_usage]
tooltip = true
label_format = "{upload}   {download} "
interval = 2000

# --- MPRIS (Media Player) ---
[widgets.mpris]
truncation_size = 20
tooltip = true
label_format = "{title} - {artist}"
hide_when_no_player = true

# --- Bluetooth ---
[widgets.bluetooth]
label = true
tooltip = true

# --- Brightness ---
[widgets.brightness]
tooltip = true
step_size = 5

# --- Microphone ---
[widgets.microphone]
label = false
tooltip = true
show_icon = true

# --- Clipboard ---
[widgets.clipboard]
icon = ""
label = false
tooltip = true
show_images = true
enable_pinning = true

# --- Wallpaper ---
[widgets.wallpaper]
icon = "󰸉"
label = false
tooltip = true

# --- Settings ---
[widgets.settings]
icon = "󰒓"
tooltip = true
label = false

# --- Theme Switcher ---
[widgets.theme_switcher]
icon = ""
notify = false

# --- Emoji Picker ---
[widgets.emoji_picker]
icon = ""
label = false
tooltip = true
per_row = 9
per_column = 4

# --- Screenshot ---
[widgets.screenshot]
path = "Pictures/Screenshots"
icon = "󰄀"
tooltip = true
annotation = true

# --- Screen Recording ---
[widgets.recorder]
path = "Videos/Screencasting"
tooltip = true
audio = true

# --- Taskbar ---
[widgets.taskbar]
icon_size = 22
tooltip = true

# --- Workspace Overview Button ---
[widgets.overview_button]
icon = "󰡃"
tooltip = true
label = false

# --- Submap Indicator ---
[widgets.submap]
icon = "󰌌"
label = true
tooltip = true
show_icon = false
hide_on_default = false

# --- World Clock ---
[widgets.world_clock]
icon = "󰃰"
use_24hr = true
show_icon = true
timezones = ["America/New_York", "Asia/Tokyo"]

# --- Custom Button Group ---
[widgets.custom_button_group]
spacing = 4

[[widgets.custom_button_group.buttons]]
command = "firefox"
icon = "󰈹"
label_text = "Firefox"
tooltip_text = "Open Firefox Browser"
show_icon = true
label = true
tooltip = true

# ── Matugen Theme Generation ─────────────────────────
# Uncomment and configure to auto-generate colors from your wallpaper.

# [matugen]
# enabled = true
# wallpaper = "~/Pictures/wallpaper.jpg"
# scheme = "scheme-tonal-spot"
# mode = "dark"
# contrast = 0.0

# ── Examples: Named Custom Widgets ───────────────────
# Define custom widgets that run shell commands.

# [widgets."custom/temperature"]
# exec = "sensors -j | jq '.[] | to_entries[] | select(.key | contains(\"temp\")).value[\"temp1_input\"]'"
# interval = 30
# label_format = "{:.1f}°C"
# tooltip = true
# on_click = "kitty -e htop"
