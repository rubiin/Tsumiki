---
title: Installation
description: Get started with Tsumiki.
sidebar:
  order: 2
---

import LatestRelease from '../../../../components/LatestRelease.astro'

<div align="center"><img src="https://raw.githubusercontent.com/rubiin/tsumiki/refs/heads/master/assets/images/tsumiki.png" /><br /></div>

::::caution
You need a working Hyprland setup before installing Tsumiki.
::::

Tsumiki is a modular panel for Hyprland with widget-based customization.

## Latest Release

<div className="latest-release">
  <LatestRelease
    repo="rubiin/tsumiki"
    versionLabel="Version:"
    dateLabel="Date:"
  />
</div>

For full release notes and assets, see the <a
	href="https://github.com/rubiin/tsumiki/releases"
	className="underline"
	target="_blank"
	rel="noopener noreferrer">
	releases page</a> on GitHub.

## Clone

```sh
git clone https://github.com/rubiin/tsumiki.git ~/.config/tsumiki
cd ~/.config/tsumiki
```

## Installation Options

Choose one method.

### Option 1: Bootstrap Script (Fastest)

```sh
curl -fsSL https://raw.githubusercontent.com/rubiin/tsumiki/master/install.sh | bash
```

This script clones or updates `~/.config/tsumiki`, then runs setup.

To also start immediately:

```sh
curl -fsSL https://raw.githubusercontent.com/rubiin/tsumiki/master/install.sh | bash -s -- --start
```

### Option 2: Automated Setup in Existing Clone

```sh
./init.sh -setup
./init.sh -start
```

`-setup` installs required packages and configures the Python environment.

### Option 3: Manual Dependency Setup

If you prefer manual package control, install dependencies yourself, then run:

```sh
./init.sh -start
```

#### Required `pacman` Packages

```sh
sudo pacman -S --noconfirm --needed pipewire playerctl dart-sass power-profiles-daemon networkmanager brightnessctl pkgconf wf-recorder kitty python pacman-contrib gtk3 cairo gtk-layer-shell libgirepository noto-fonts-emoji gobject-introspection gobject-introspection-runtime python-pip python-gobject python-psutil python-cairo python-loguru libnotify cliphist python-httpx satty nvtop
```

#### Required AUR Packages

```sh
yay -S --needed python-fabric-git gnome-bluetooth-3.0 python-rlottie-python python-pytomlpp slurp imagemagick tesseract tesseract-data-eng ttf-jetbrains-mono-nerd grimblast-git python-ijson glace-git
```

If you do not use `yay`, install the same AUR packages with your preferred helper.

## After Install

Continue with [Post Installation](/en/resources/post-install) and [Configuration](/en/configuring/config).

::::tip
Contributions to docs and install scripts are welcome:
[Tsumiki on GitHub](https://github.com/rubiin/tsumiki)
::::
