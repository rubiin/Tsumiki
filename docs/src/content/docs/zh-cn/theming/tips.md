---
title: 主题化技巧
description: Tsumiki 的实用主题化建议
---

## 保持高对比度

文本在所有表面上都应保持可读。

## 测试常见表面

主状态栏、快速设置、通知、启动器、Dock。

## 使用语义强调色

将强烈的颜色留给重要状态。

## Matugen 工作流程

1. 在 `config.toml` 中设置 `matugen.enabled = true`
2. 将 `wallpaper` 指向您的图片
3. 重启 Tsumiki
4. 如有需要重新编译：`./tsumiki.sh -recompile`
