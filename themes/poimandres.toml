[dark.background]
main = "#1f1f1f" # Main background (deep charcoal)
alt = "#252525" # Secondary background (slightly lighter charcoal)
dark = "#121212" # Darkest background (almost black)


[dark.text]
main = "#eaeaea" # Primary text color (light gray)
secondary = "#8d8d8d" # Secondary text color (gray)
muted = "#5f5f5f" # Tertiary text, muted (dusty gray)
disabled = "#6c6c6c" # Disabled text (muted gray)
muted-light = "#808080" # Light-muted text for hints (medium gray)
muted-dark = "#444444" # Dark-muted text (dark gray)


[dark.surface]
disabled = "#4e4e4e" # Disabled items (dark gray)
neutral = "#3e3e3e" # Neutral surface for cards, panels (darker gray)
highlight = "#6aa1ff" # Selection and highlights (light cyan)
popover-border-color = "rgba(46, 46, 46, 0.6)" # Popover borders


[dark.accent]
light = "#ffb86c" # Light accent (soft amber)
pink = "#ff79c6" # Pink accent (neon pink)
purple = "#bd93f9" # Purple accent (light purple)
red = "#ff5555" # Red for errors and warnings (bright red)
orange = "#ffb86c" # Orange for alerts (light amber)
yellow = "#f1fa8c" # Yellow for highlights (pale yellow)
green = "#50fa7b" # Green for success (vibrant green)
teal = "#8be9fd" # Teal for information (soft cyan)
blue = "#6272a4" # Blue for links and actions (muted blue)
light-blue = "#8be9fd" # Sky blue accent (soft cyan)
lavender = "#bd93f9" # Lavender for subtle highlights (soft purple)


[dark.general]
bar-background = "rgba(31, 31, 31, 0.8)" # Panel background (semi-transparent dark)
shadow-color = "rgba(0, 0, 0, 0.7)" # Shadow color (soft, dark shadows)


[light.background]
main = "#e0e0e0" # Main background (deep charcoal)
alt = "#dadada" # Secondary background (slightly lighter charcoal)
dark = "#ededed" # Darkest background (almost black)


[light.text]
main = "#151515" # Primary text color (light gray)
secondary = "#727272" # Secondary text color (gray)
muted = "#a0a0a0" # Tertiary text, muted (dusty gray)
disabled = "#939393" # Disabled text (muted gray)
muted-light = "#7f7f7f" # Light-muted text for hints (medium gray)
muted-dark = "#bbbbbb" # Dark-muted text (dark gray)


[light.surface]
disabled = "#b1b1b1" # Disabled items (dark gray)
neutral = "#c1c1c1" # Neutral surface for cards, panels (darker gray)
highlight = "#955e00" # Selection and highlights (light cyan)
popover-border-color = "rgba(106, 106, 106, 0.6)" # Popover borders


[light.accent]
light = "#004793" # Light accent (soft amber)
pink = "#008639" # Pink accent (neon pink)
purple = "#426c06" # Purple accent (light purple)
red = "#00aaaa" # Red for errors and warnings (bright red)
orange = "#004793" # Orange for alerts (light amber)
yellow = "#0e0573" # Yellow for highlights (pale yellow)
green = "#af0584" # Green for success (vibrant green)
teal = "#741602" # Teal for information (soft cyan)
blue = "#9d8d5b" # Blue for links and actions (muted blue)
light-blue = "#741602" # Sky blue accent (soft cyan)
lavender = "#426c06" # Lavender for subtle highlights (soft purple)


[light.general]
bar-background = "rgba(224, 224, 224, 0.8)" # Panel background (semi-transparent dark)
shadow-color = "rgba(255, 255, 255, 0.7)" # Shadow color (soft, dark shadows)
